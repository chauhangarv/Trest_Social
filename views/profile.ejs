<% include ./partials/header.ejs %>

<div class="profile w-full min-h-screen bg-zinc-500 text-white pt-1">
    <form id="uploadform" hidden action="/fileupload" method="post" enctype="multipart/form-data">
        <input type="file" name="image">
    </form>
    <div class="profdets flex flex-col items-center mt-20">
        <div class="relative">
            <span id="uploadicon" class="w-8 h-8 absolute bottom-0 right-0 rounded-full flex items-center justify-center bg-zinc-200">
                <i class="text-zinc-800 ri-pencil-fill"></i>
            </span>
              <div class="w-32 h-32 bg-zinc-200 rounded-full overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profileImage %>" alt="">
            </div>
        </div>
        <h1 class="text-2xl mt-3 font-medium"><%= user.name %></h1>
        <h3 class="text-md">@<%= user.username %></h3>
        <div class="flex space-x-4">
            <a href="/edit" class="px-7 mt-3 py-2 bg-zinc-700 rounded-full text-sm font-medium">Edit</a>
            <a href="/add" class="px-10 py-2 rounded-full bg-red-600 text-sm font-medium mt-2">Add New Post</a>
        </div>
    </div>

    <div class="cards flex flex-wrap gap-10 px-10 mt-8">
        <div class="cards">
          <div class="w-52 h-40 bg-zinc-200 rounded-lg overflow-hidden">
            <img class="w-full h-full object-cover" src="/images/uploads/<%= user?.posts[0]?.image %>" alt="">
          </div>
          <a href="/show/posts" class="inline-block text-xl font-medium mt-2">Your Uploaded Pins</a>
          <h5 class="text-sm font-normal opacity-80"><%= user.posts.length %></h5>
        </div>
    </div>
</div>

<script>
    document.querySelector("#uploadicon").addEventListener("click", function () {
        document.querySelector("#uploadform input").click();
    })

document.querySelector("#uploadform input").addEventListener("change", function() {
    document.querySelector("#uploadform").submit();
})
</script>

<% include ./partials/footer.ejs %>